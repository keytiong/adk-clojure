---
phase: 01-ship-it
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - core/src/main/clojure/io/kosong/adk/generated_types.clj
autonomous: true
requirements:
  - GEN-01
  - GEN-03
  - BUILD-01
  - BUILD-02
must_haves:
  truths:
    - "generated_types.clj is tracked by git (not untracked)"
    - "Running generate-types twice produces identical output (idempotent)"
    - "jar task completes without errors after generate-types"
    - "jar task does not silently regenerate the file"
  artifacts:
    - path: "core/src/main/clojure/io/kosong/adk/generated_types.clj"
      provides: "Committed generated type conversions"
      contains: "ns io.kosong.adk.generated-types"
  key_links:
    - from: "core/build.clj jar function"
      to: "core/src/main/clojure/io/kosong/adk/generated_types.clj"
      via: "b/copy-dir on src/main/clojure"
      pattern: "copy-dir.*src/main/clojure"
---

<objective>
Track generated_types.clj in git and verify the full build pipeline end-to-end.

Purpose: Ensure the generated file is committed (GEN-01), generation is idempotent (GEN-03), and the jar build works cleanly (BUILD-01, BUILD-02).
Output: generated_types.clj tracked in git, verified idempotent generation, verified jar build
</objective>

<execution_context>
@/home/keytiong/.claude/get-shit-done/workflows/execute-plan.md
@/home/keytiong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ship-it/01-RESEARCH.md
@.planning/phases/01-ship-it/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify idempotency and track generated_types.clj in git</name>
  <files>core/src/main/clojure/io/kosong/adk/generated_types.clj</files>
  <read_first>
    - core/build.clj
    - core/src/main/clojure/io/kosong/adk/generated_types.clj (first 20 lines to confirm ns form)
  </read_first>
  <action>
Verify idempotency (GEN-03) then track the file (GEN-01):

1. Run generate-types and capture a copy:
```bash
cd /home/keytiong/Projects/adk-clojure/core
clojure -T:build generate-types
cp src/main/clojure/io/kosong/adk/generated_types.clj /tmp/gen1.clj
```

2. Run generate-types a second time and diff:
```bash
clojure -T:build generate-types
diff /tmp/gen1.clj src/main/clojure/io/kosong/adk/generated_types.clj
```
The diff MUST be empty. If it is not, investigate and fix the non-determinism before proceeding.

3. Stage the generated file in git:
```bash
cd /home/keytiong/Projects/adk-clojure
git add core/src/main/clojure/io/kosong/adk/generated_types.clj
```

Do NOT commit yet — that happens at the end of plan execution.

4. Verify BUILD-01 — confirm jar does NOT call generate-types. Read `core/build.clj` and verify the `jar` function body contains only `compile-java`, `write-pom`, `copy-dir`, and `jar` calls. No call to `generate-types` or `generate-source-file` exists in the `jar` function.

5. Verify BUILD-02 — end-to-end build:
```bash
cd /home/keytiong/Projects/adk-clojure/core
clojure -T:build jar
```
Must complete without errors. The JAR file should be created at `core/target/adk-clojure-0.1.0-SNAPSHOT.jar`.
  </action>
  <verify>
    <automated>cd /home/keytiong/Projects/adk-clojure/core && clojure -T:build generate-types && cp src/main/clojure/io/kosong/adk/generated_types.clj /tmp/gen1.clj && clojure -T:build generate-types && diff /tmp/gen1.clj src/main/clojure/io/kosong/adk/generated_types.clj && echo "IDEMPOTENT" && clojure -T:build jar && ls -la target/adk-clojure-0.1.0-SNAPSHOT.jar && echo "JAR_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `diff` of two consecutive generate-types runs produces empty output
    - `git status core/src/main/clojure/io/kosong/adk/generated_types.clj` shows the file as staged (new file or tracked)
    - `core/target/adk-clojure-0.1.0-SNAPSHOT.jar` exists after `clojure -T:build jar`
    - `jar` function in build.clj does NOT contain the string "generate-types" or "generate-source-file"
    - generated_types.clj first line contains `(ns io.kosong.adk.generated-types`
  </acceptance_criteria>
  <done>generated_types.clj is staged in git, generation is proven idempotent, jar build succeeds without errors</done>
</task>

</tasks>

<verification>
- `git diff --cached --name-only` includes `core/src/main/clojure/io/kosong/adk/generated_types.clj`
- Two consecutive `generate-types` runs produce identical output
- `clojure -T:build jar` succeeds and produces JAR file
- `clojure -X:test` still passes (regression check from Plan 01)
</verification>

<success_criteria>
generated_types.clj is tracked in git. Idempotency verified. JAR build succeeds end-to-end. No silent regeneration in jar task.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ship-it/01-02-SUMMARY.md`
</output>
